"use client";

import { Sun } from "./Sun";

/** Act 7 — the sunrise finale. The sky, grade and fog resolve through the
 *  global environment; this scene contributes the sun disc + god rays. */
export function DawnScene() {
  return <Sun />;
}
